{
  "name": "counterpart-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "serverless-http": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
